/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel의 Next.js 빌더가 App Router 라우트를 자동으로 배포하도록 둡니다.
  // 정적 HTML export가 필요하면 로컬/별도 호스팅용으로만 `output: 'export'`를 다시 켜세요.
};

export default nextConfig;
